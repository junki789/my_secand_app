<% if @posts.errors.any? %>
  <div class="alert alert-warning">
    <ul>
      <% @posts.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>